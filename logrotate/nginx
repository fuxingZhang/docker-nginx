/var/log/nginx/*.log {
  daily
	missingok
	rotate 52
	compress
	notifempty
	create 640 nginx adm
	sharedscripts
  su nginx adm

	postrotate
		if [ -f /var/run/nginx.pid ]; then
			kill -USR1 `cat /var/run/nginx.pid`
		fi
	endscript
}